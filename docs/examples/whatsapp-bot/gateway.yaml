port: 3800

apps:
  - name: my-shop
    config: apps/my-shop/app.yaml
    channels:
      - type: whatsapp
        multiTenant: true
        verifyTokenEnv: MY_WHATSAPP_WEBHOOK_SECRET
        publicMediaBaseUrlEnv: GATEWAY_PUBLIC_URL
        publicMediaSigningSecretEnv: GATEWAY_MEDIA_SIGNING_SECRET
