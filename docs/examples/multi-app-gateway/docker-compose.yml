services:
  gateway:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - kiln-data:/root/.kiln/gateway
      - ${KILN_AUTH_DIR:?Set KILN_AUTH_DIR to your host ~/.kiln/auth directory}:/root/.kiln/auth:ro
    restart: unless-stopped

volumes:
  kiln-data:
