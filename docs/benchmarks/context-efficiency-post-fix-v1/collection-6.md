# Context Efficiency Control 6

Status: terminal, diagnostic-only; both pending economic reservations reconciled.

This cohort tests the managed economic admission repair first, then retains the
remaining task order, conditions, repetitions, scoring, and per-trial budgets
from Control 5. The changed order is declared before freezing or dispatch.
Results remain a separate diagnostic cohort; do not pool with prior trials.

The Plus-only codex-luna route and existing cumulative authorization remain.
The collection has at most 116 physical provider requests: 532 previously known
plus 17 retained reservations plus 116 equals the approved maximum of 665.
The previous full collection used 150 requests, so this collection may stop
at the budget boundary before completing every scheduled row. A stopped or
invalid row remains visible; a partial cohort is not a valid full control.

The launcher applies the previously authorized exact file grants through the
ordinary CLI, verifies permissions and execution identity, and restores those
grants in its finalizer. Mutable execution evidence belongs in the execution
project's private benchmarks/context-efficiency-control-6 namespace.

Frozen source: `1a75522610472686f034bef24b3b0a265af686dd`.
Frozen manifest digest: `sha256:619559d37fbd4672e4a7776bc704f5f2cab8200184c646d02ceac0693e2b3662`.
Execution began 2026-09-05T22:55:59.649Z after refreshed Plus usage and successful
identity verification. No benchmark requests were dispatched during the two
failed setup attempts; both restored temporary grants exactly. The first setup
attempt overwrote several Control5 wrapper logs through a stale launcher path.
Its frozen manifest, report, terminal review, and repair evidence remain intact;
the private wrapper-log-integrity-notice.json records the evidence gap.

The launcher ended at 2026-09-05T23:08:32.149Z with exact grant restoration.
The report records 27 attempts: 21 valid (13 pass, 8 fail) and 6 invalid.
Coding passed 6/6; managed-child success was 0. It used 110 physical requests
with no new unknown requests, leaving 6 of the existing authorization. The next
trial requires an 8-request allocation, so collection stopped before completion.
Two global reservations were left pending with runtime-prestart-validation-failed.
On 2026-09-07, the canonical operator reconciliation command released both as
`not-dispatched` using the reviewed frozen-source and retained denial evidence.
Read-only ledger verification confirmed both leases released, both dispatch fences
preserved, and zero remaining pending commitments or held economic leases.
Recovery evidence remains in the private collection's `reconciliation` directory.
This recovery does not change the benchmark results or request allowance.
See the [startup diagnosis](../../research/active/managed-child-startup-diagnosis.md).
