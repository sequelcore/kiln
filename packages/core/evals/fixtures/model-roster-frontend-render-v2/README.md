# Frontend interaction fixture

Implement the task supplied by the benchmark in `src/Challenge.jsx`. The app
entrypoint and styles are trusted verifier inputs and must not be changed. Use
native semantic controls where possible and satisfy the named keyboard, focus,
state, and accessibility behavior. Change only the challenge component.
