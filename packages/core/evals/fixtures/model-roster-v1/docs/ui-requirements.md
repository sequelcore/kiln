# Checkout Summary Requirements

- The clear-cart control must use `type="button"` inside the form.
- List items must use the stable `line.id` as their React key.
- A checkout error must expose `role="alert"` so assistive technology announces it.
