These files are the pre-ADR-006 single-file UI scaffolded by the initial Codex pass. They are kept as reference for Phase 1 parity porting — `app.tsx.ref` contains the monolithic application shell with session management, provider cycling, command palette, and transcript rendering; `styles.css.ref` contains the legacy CSS. Do not import these files from anywhere in the package; they are archived for porting reference only and will be deleted once Phase 1 parity is confirmed complete.
