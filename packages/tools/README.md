# @kilnai/tools

Platform-aware resolver for Kiln vendored developer tool binaries.

This package resolves candidate platform packages for `rg`, `fd`, and `jq`.
It does not execute binaries and does not modify runtime tool selection by itself.
