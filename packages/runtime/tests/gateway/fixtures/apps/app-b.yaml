name: test-app-b
channels: [api]

memory:
  scopes: [user]
  backend: sqlite

router:
  rules: []
  fallback: default

teams:
  default:
    agents:
      worker:
        name: Worker Agent
        role: software engineer
        goal: Execute assigned tasks
        tier: coding
        tools: []
    workflow:
      phases: [execute]
      gates: {}
    capabilities:
      - name: test_tool
        description: A test tool
        tags: [test]
    qualityGates:
      - name: test
        command: "echo ok"
        description: Test gate
        required: true
