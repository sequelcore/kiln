name: test-app-a


router:
  fallback: default

teams:
  default:
    agents:
      worker:
        name: Worker Agent
        role: software engineer
        goal: Execute assigned tasks
        tier: coding
        tools: []
    capabilities:
      - name: test_tool
        description: A test tool
        tags: [test]
