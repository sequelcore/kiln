{
  "name": "kiln",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "tsc -b packages/core packages/runtime packages/cli",
    "test": "bun run --filter '*' test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.45"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18"
  }
}
