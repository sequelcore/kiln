{
  "name": "kiln",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "tsc -b packages/core packages/runtime packages/sdk packages/cli && tsc -p packages/studio/tsconfig.json --noEmit",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.9",
    "better-sqlite3": "^12.6.2",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18"
  }
}
