{
  "name": "kiln",
  "description": "Kiln control-plane monorepo",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "tsc -b packages/gateway-contracts packages/tools packages/core packages/runtime packages/sdk packages/cli packages/tui packages/native && tsc -p packages/widget/tsconfig.json --noEmit && tsc -p packages/studio/tsconfig.json --noEmit && tsc -p packages/gui/tsconfig.json --noEmit && tsc -p scripts/tsconfig.json --noEmit",
    "test": "bun run --filter @kilnai/gateway-contracts test && bun run --filter @kilnai/core test && bun run --filter @kilnai/runtime test && bun run --filter @kilnai/cli test && bun run --filter @kilnai/react test && bun run --filter @kilnai/widget test && bun run --filter @kilnai/tui test && bun run --filter @kilnai/native test && bun run --filter @kilnai/studio test && bun run --filter @kilnai/gui test",
    "test:e2e": "bun run --cwd packages/gui test:e2e",
    "profile:startup": "bun run scripts/profile-startup.ts",
    "test:harness": "bun run scripts/run-managed-agent-harness-tests.ts",
    "test:managed-agents:live": "bun run scripts/run-managed-agent-live-tests.ts",
    "vendor:tools": "bun run scripts/vendor-tools.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.9",
    "@types/react": "^19.0.0",
    "@vitest/coverage-v8": "^4.1.9",
    "better-sqlite3": "^12.6.2",
    "react": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@kilnai/tui": "workspace:*",
    "@opentui/core": "0.1.92",
    "@opentui/react": "0.1.92"
  }
}
