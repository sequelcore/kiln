{
  "name": "kiln",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "tsc -b packages/core packages/runtime packages/sdk packages/cli packages/tui && tsc -p packages/studio/tsconfig.json --noEmit",
    "test": "bun run --filter @kilnai/core test && bun run --filter @kilnai/runtime test && bun run --filter @kilnai/cli test && bun run --filter @kilnai/react test && bun run --filter @kilnai/widget test && bun run --filter @kilnai/studio test && bun run --filter @kilnai/tui test"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.9",
    "@types/react": "^19.0.0",
    "@vitest/coverage-v8": "^4.0.18",
    "better-sqlite3": "^12.6.2",
    "react": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@kilnai/tui": "workspace:*",
    "@opentui/core": "0.1.92",
    "@opentui/react": "0.1.92"
  }
}
