port: 3000

apps:
  - name: support
    config: apps/support/app.yaml
    channels:
      - type: web
        multiTenant: true
      - type: api
        path: /api/support
        multiTenant: true

  - name: booking
    config: apps/booking/app.yaml
    channels:
      - type: web
        multiTenant: true
      - type: api
        path: /api/booking
        multiTenant: true
